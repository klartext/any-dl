

 - Bug bei baseurl: wenn "/" am Ende, dann klappt es nicht...?!

 - Selection-Mechanism: rowselect(), colselect(), evtl. auch direkte Indizierung.
   auch mir Regexp-Selection (oder soll sowas über nochmaliges Match gemacht werden?)

 - Stringconcat()

 - Programmaufruf-Definitionen

 - system()-like ausführung von Programm-komandos

 - set / get und variablennamen set($<varname>) => Beispiel  set( $second_url ); get( $first_url); print;


 - Parserdefinitionen wenigstens für ARD, ZDF, ORF, NDR, besser auch gleich für ARTE (siehe arte-dl)

 - evtl. grep-Kommando, das zeilenweise arbeitet.... für die handhabung von href's

 - Rebase - automatisch nach linkextract?!

 - Special parsers (Match-all-URLS, oder MATCH-all-parsernames or special definitjons,
   zusammen mit CLI-Kommando, um bestimmte Parser zu aktivieren.
    Alternativ: spezielle Parser ZWANGSWEISE auswählen, also die Parser-Auswahl via
    URL disablen und einen bestimmten Parser anwenden.
    ist sinnvoll, wenn man z.B. einen Parser hat, der alle html-href-Links ausgibt,
    und er grundsätzlich auf die Ausgabe angesetzt werden soll (statt nur auf bestimmte URLs).

    => any-dl -s <parsername>
